1. _可以作为标识符来使用
2. _可以作为匿名函数的参数来使用
3. _可以在导包的时候表示导入包下所有的内容  import java.util._
4. _可以在导包的时候用来屏蔽某个包下的类  import java.sql.{ Date=>_ , _}
5. _可以作为属性的默认初始化来使用 private var password : String = _
6. _可以作为模式匹配的default分支来使用  case _
7. _可以作为泛型中的任意类型来使用 [_]
8. _可以在模式匹配中,用来表示集合中的一个任意元素